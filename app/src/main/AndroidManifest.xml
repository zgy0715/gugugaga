<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 震动权限（100ms 短震动需要） -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gugugaga">

        <!-- ═══════════════════════════════════════════════════
             启动触发器 Activity

             Activity 只做一件事：启动 AudioPlayService 后立即 finish，
             存活时间仅几毫秒，用户完全感知不到任何界面。

             android:theme="@style/Theme.Gugugaga.NoDisplay"
               → 不绘制任何窗口，连启动预览闪屏都禁用

             android:excludeFromRecents="true"
               → 不出现在最近任务列表中

             android:finishOnTaskLaunch="true"
               → 从桌面重新启动时自动关闭旧实例
             ═══════════════════════════════════════════════════ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Gugugaga.NoDisplay"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:stateNotNeeded="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ═══════════════════════════════════════════════════
             后台音效播放服务

             所有实际操作（震动 + 音频播放）都在此 Service 中执行。
             无界面、无窗口，播完后自动销毁。
             ═══════════════════════════════════════════════════ -->
        <service
            android:name=".AudioPlayService"
            android:exported="false" />
    </application>

</manifest>
